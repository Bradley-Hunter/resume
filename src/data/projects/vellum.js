export default {
  slug: 'vellum',
  title: 'Vellum Web Browser',
  order: 1,
  featured: true,
  date: 'Jan 2026',
  summary: 'A tabbed web browser built with Electron, React, and TypeScript, designed around tiling split panes for large monitors. Features workspace management, multi-window support, cloud sync via a Rust API server, and a three-tier IPC security model.',
  description:
    "Vellum is a tabbed web browser I built as a desktop application with Electron, React, and TypeScript. The idea started with a simple frustration: on a 32-inch monitor, most websites constrain their content width, which means a single tab wastes more than half the screen. I wanted a browser where I could tile multiple sites into a grid and switch between independent workspaces that each remembered their own layout. Beyond the tiling, I gathered feature requirements from a professor and a recruiter to figure out what people would actually expect from a daily-driver browser, then prioritized those into must-have, stretch, and probably-not tiers before building anything.\n\nThe tiling system is the core of the project and the first thing I built. Workspace layouts are modeled as recursive binary trees where each leaf is a browser view and each container is a split with a direction and ratio. The tree enforces constraints — a max depth of two and alternating split directions — to keep layouts in a clean grid rather than allowing degenerate nesting. When a pane is closed, the tree dynamically collapses so the remaining content fills the space. The result is up to four panes per workspace in a 2×2 grid, with draggable dividers between them.\n\nEach workspace maintains its own layout tree, and the app supports multiple independent windows, each with its own set of workspaces. Getting to multi-window support required a significant refactor because the original architecture used global state that assumed a single window. Introducing a second window meant that IPC calls, layout operations, and view lifecycle events all needed to know which window they belonged to. The solution was a global window registry that maps each window to its own layout manager and view manager, so operations that previously touched global state now route through the registry to the correct window. That refactor touched most of the codebase and changed how nearly every subsystem communicated, but it also cleaned up assumptions that had been accumulating since the early prototypes.\n\nThe cloud sync feature came from a conversation with a recruiter where I asked him what he would want from a browser if he were using it as a daily driver. I had a fully functional local browser at that point, and cross-device sync was at the top of his list. So I built a separate API server in Rust using Axum and PostgreSQL to handle account authentication, bookmark and history sync, and device tab sharing. The sync uses a last-write-wins strategy with soft-delete tombstones so that deletions propagate across devices instead of reappearing on the next sync cycle. Each synced record carries a UUID for cross-device identity and an updated_at timestamp for conflict resolution. I chose last-write-wins deliberately because it kept the server simple — no merge logic, no conflict queues, just timestamp comparison — and for a personal browser the edge case where two devices edit the same bookmark within the same 45-second sync window is rare enough that the tradeoff made sense. You can see what tabs are open on your other devices and send a tab from one machine to another, which gets picked up on the next sync cycle and opens in a new workspace.\n\nThe Electron side uses a three-tier IPC architecture where the renderer process is fully sandboxed with no direct access to Node.js or Electron APIs. Every method the renderer can call is explicitly declared in a typed preload bridge — roughly 50 methods organized by domain — which means no API surface is accidentally exposed. This is more boilerplate than most Electron apps bother with, but the alternative is a renderer that can reach anything in Node.js, which is a real attack surface for an application that loads arbitrary web content. Every new feature requires wiring up the preload layer, the IPC handler, and the store logic separately, but that friction is intentional because it forces each piece to only expose what it actually needs.\n\nSession persistence saves and restores the full state of every window, every workspace, every layout tree with its split ratios, and every active URL across restarts. The auto-update system checks GitHub Releases on a regular interval, downloads updates in the background, displays release notes inside the app, shows a badge on the menu when an update is ready, and installs on quit without requiring a manual restart — which meant managing an update state machine that tracks checking, downloading, ready, and error states without blocking the user from browsing. Beyond those, the features that turned Vellum into something I could actually use daily include bookmarks with folder organization, searchable browsing history with auto-pruning at 10,000 entries, per-site permission management for geolocation, notifications, and media access, a built-in text editor for code and text files, and an in-app issue reporting system that files GitHub issues automatically. The scope grew well beyond what the capstone required, but each addition either came from the requirements I gathered early on or from problems I ran into while using Vellum as my own daily browser.",
  tools: ['TypeScript', 'React', 'Electron', 'Rust', 'Axum', 'PostgreSQL', 'Vite', 'Tailwind CSS', 'Zustand', 'SQLite', 'Vitest', 'React Testing Library', 'ESLint', 'Claude Code'],
  github: 'https://github.com/Bradley-Hunter/vellum-releases',
  githubNote: 'Links to the public releases repository — source code is private.',
  files: [],
  media: null,
}
